# Gradient Generator

Gradient Generator allows users to create simple and aesthetic CSS color gradients. The direction of the gradient can be changed, color values can be toggled between HEX and RGB, and the resulting CSS code can be directly copied to the clipboard.
